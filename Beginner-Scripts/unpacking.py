
# Unpacking extracts info from a table and allocates it to a specific variable. 

# List the info in coordinates 
coordinates = (1, 2, 3)
# Allocate x,y,z to "coordinates"
x, y, z = coordinates
print(x, y, z)

# List the info in coordinates 
list = [1, 2, 3]
# Allocate H,L,C to "list"
H, L, C = list
print(H, L, C)
